import React from 'react';
import { Delete, Search, Home, Edit, Close, Github, ChevronDoubleRight, ChevronDoubleLeft, ChevronDown, ChevronNext, Code, Chip, Account, Sun, Moon, Star, StarOutline, Smartphone, Hammer, City, ChevronBack, Empty, Download, Menu, Person, Loading, ShieldAccount, ReactLogo, FileOutline, Email, Rocket, Key, ExitToApp, ArrowUpBold, FormatAlignCenter, FormatAlignLeft, FormatAlignRight, Brush, Lock, Filter, SortAscending, SortDescending, ContentCopy, ToyBrick, ChevronUp, HelpCircleOutline, DotsVertical, DotsHorizontal, LanguageCss3, Earth, Check, Eye, EyeOff, Tenge, CurrencyUsd, CreditCard, QrCode, CalendarMonth, CardAccountDetails, Translate, ArrowDownBold, Fullscreen, FullscreenExit, Alert, CalendarStar, Shape, FormatSize, BookOpen, CogOutline, Target, Map, MapMarker, LanguageJS, LanguageTS, LanguageGo, Gitlab, LinkedIn, Facebook, FacebookWorkplace, Instagram, WhatsApp, Telegram, Flag, StarHalfFull, Plus, AccountPlus, Share, Phone, Bell, Wrench, Minus, CheckCircle } from './icons/index';

var Icon = function Icon(props) {
  var Icon = function Icon() {
    switch (props.name) {
      case 'delete':
        return React.createElement(Delete, {
          size: props.size,
          color: props.color
        });

      case 'search':
        return React.createElement(Search, {
          size: props.size,
          color: props.color
        });

      case 'home':
        return React.createElement(Home, {
          size: props.size,
          color: props.color
        });

      case 'edit':
        return React.createElement(Edit, {
          size: props.size,
          color: props.color
        });

      case 'github':
        return React.createElement(Github, {
          size: props.size,
          color: props.color
        });

      case 'close':
        return React.createElement(Close, {
          size: props.size,
          color: props.color
        });

      case 'chevron-double-right':
        return React.createElement(ChevronDoubleRight, {
          size: props.size,
          color: props.color
        });

      case 'chevron-double-left':
        return React.createElement(ChevronDoubleLeft, {
          size: props.size,
          color: props.color
        });

      case 'code':
        return React.createElement(Code, {
          size: props.size,
          color: props.color
        });

      case 'chip':
        return React.createElement(Chip, {
          size: props.size,
          color: props.color
        });

      case 'account':
        return React.createElement(Account, {
          size: props.size,
          color: props.color
        });

      case 'person':
        return React.createElement(Person, {
          size: props.size,
          color: props.color
        });

      case 'chevron-down':
        return React.createElement(ChevronDown, {
          size: props.size,
          color: props.color
        });

      case 'chevron-next':
        return React.createElement(ChevronNext, {
          size: props.size,
          color: props.color
        });

      case 'chevron-back':
        return React.createElement(ChevronBack, {
          size: props.size,
          color: props.color
        });

      case 'sun':
        return React.createElement(Sun, {
          size: props.size,
          color: props.color
        });

      case 'moon':
        return React.createElement(Moon, {
          size: props.size,
          color: props.color
        });

      case 'star':
        return React.createElement(Star, {
          size: props.size,
          color: props.color
        });

      case 'star-outline':
        return React.createElement(StarOutline, {
          size: props.size,
          color: props.color
        });

      case 'smartphone':
        return React.createElement(Smartphone, {
          size: props.size,
          color: props.color
        });

      case 'hammer':
        return React.createElement(Hammer, {
          size: props.size,
          color: props.color
        });

      case 'city':
        return React.createElement(City, {
          size: props.size,
          color: props.color
        });

      case 'empty':
        return React.createElement(Empty, {
          size: props.size,
          color: props.color
        });

      case 'download':
        return React.createElement(Download, {
          size: props.size,
          color: props.color
        });

      case 'menu':
        return React.createElement(Menu, {
          size: props.size,
          color: props.color
        });

      case 'loading':
        return React.createElement(Loading, {
          size: props.size,
          color: props.color
        });

      case 'shield-account':
        return React.createElement(ShieldAccount, {
          size: props.size,
          color: props.color
        });

      case 'react':
        return React.createElement(ReactLogo, {
          size: props.size,
          color: props.color
        });

      case 'file-outline':
        return React.createElement(FileOutline, {
          size: props.size,
          color: props.color
        });

      case 'email':
        return React.createElement(Email, {
          size: props.size,
          color: props.color
        });

      case 'rocket':
        return React.createElement(Rocket, {
          size: props.size,
          color: props.color
        });

      case 'key':
        return React.createElement(Key, {
          size: props.size,
          color: props.color
        });

      case 'exit-to-app':
        return React.createElement(ExitToApp, {
          size: props.size,
          color: props.color
        });

      case 'arrow-up-bold':
        return React.createElement(ArrowUpBold, {
          size: props.size,
          color: props.color
        });

      case 'format-align-center':
        return React.createElement(FormatAlignCenter, {
          size: props.size,
          color: props.color
        });

      case 'format-align-left':
        return React.createElement(FormatAlignLeft, {
          size: props.size,
          color: props.color
        });

      case 'format-align-right':
        return React.createElement(FormatAlignRight, {
          size: props.size,
          color: props.color
        });

      case 'brush':
        return React.createElement(Brush, {
          size: props.size,
          color: props.color
        });

      case 'lock':
        return React.createElement(Lock, {
          size: props.size,
          color: props.color
        });

      case 'filter':
        return React.createElement(Filter, {
          size: props.size,
          color: props.color
        });

      case 'sort-ascending':
        return React.createElement(SortAscending, {
          size: props.size,
          color: props.color
        });

      case 'sort-descending':
        return React.createElement(SortDescending, {
          size: props.size,
          color: props.color
        });

      case 'content-copy':
        return React.createElement(ContentCopy, {
          size: props.size,
          color: props.color
        });

      case 'toy-brick':
        return React.createElement(ToyBrick, {
          size: props.size,
          color: props.color
        });

      case 'chevron-up':
        return React.createElement(ChevronUp, {
          size: props.size,
          color: props.color
        });

      case 'help-circle-outline':
        return React.createElement(HelpCircleOutline, {
          size: props.size,
          color: props.color
        });

      case 'dots-vertical':
        return React.createElement(DotsVertical, {
          size: props.size,
          color: props.color
        });

      case 'dots-horizontal':
        return React.createElement(DotsHorizontal, {
          size: props.size,
          color: props.color
        });

      case 'language-css-3':
        return React.createElement(LanguageCss3, {
          size: props.size,
          color: props.color
        });

      case 'earth':
        return React.createElement(Earth, {
          size: props.size,
          color: props.color
        });

      case 'check':
        return React.createElement(Check, {
          size: props.size,
          color: props.color
        });

      case 'eye':
        return React.createElement(Eye, {
          size: props.size,
          color: props.color
        });

      case 'eye-off':
        return React.createElement(EyeOff, {
          size: props.size,
          color: props.color
        });

      case 'tenge':
        return React.createElement(Tenge, {
          size: props.size,
          color: props.color
        });

      case 'currency-usd':
        return React.createElement(CurrencyUsd, {
          size: props.size,
          color: props.color
        });

      case 'credit-card':
        return React.createElement(CreditCard, {
          size: props.size,
          color: props.color
        });

      case 'qrcode':
        return React.createElement(QrCode, {
          size: props.size,
          color: props.color
        });

      case 'calendar-month':
        return React.createElement(CalendarMonth, {
          size: props.size,
          color: props.color
        });

      case 'card-account-details':
        return React.createElement(CardAccountDetails, {
          size: props.size,
          color: props.color
        });

      case 'translate':
        return React.createElement(Translate, {
          size: props.size,
          color: props.color
        });

      case 'arrow-down-bold':
        return React.createElement(ArrowDownBold, {
          size: props.size,
          color: props.color
        });

      case 'fullscreen':
        return React.createElement(Fullscreen, {
          size: props.size,
          color: props.color
        });

      case 'fullscreen-exit':
        return React.createElement(FullscreenExit, {
          size: props.size,
          color: props.color
        });

      case 'alert':
        return React.createElement(Alert, {
          size: props.size,
          color: props.color
        });

      case 'calendar-star':
        return React.createElement(CalendarStar, {
          size: props.size,
          color: props.color
        });

      case 'shape':
        return React.createElement(Shape, {
          size: props.size,
          color: props.color
        });

      case 'format-size':
        return React.createElement(FormatSize, {
          size: props.size,
          color: props.color
        });

      case 'book-open':
        return React.createElement(BookOpen, {
          size: props.size,
          color: props.color
        });

      case 'cog-outline':
        return React.createElement(CogOutline, {
          size: props.size,
          color: props.color
        });

      case 'target':
        return React.createElement(Target, {
          size: props.size,
          color: props.color
        });

      case 'map':
        return React.createElement(Map, {
          size: props.size,
          color: props.color
        });

      case 'map-marker':
        return React.createElement(MapMarker, {
          size: props.size,
          color: props.color
        });

      case 'language-js':
        return React.createElement(LanguageJS, {
          size: props.size,
          color: props.color
        });

      case 'language-ts':
        return React.createElement(LanguageTS, {
          size: props.size,
          color: props.color
        });

      case 'language-go':
        return React.createElement(LanguageGo, {
          size: props.size,
          color: props.color
        });

      case 'gitlab':
        return React.createElement(Gitlab, {
          size: props.size,
          color: props.color
        });

      case 'linkedin':
        return React.createElement(LinkedIn, {
          size: props.size,
          color: props.color
        });

      case 'facebook':
        return React.createElement(Facebook, {
          size: props.size,
          color: props.color
        });

      case 'facebook-workplace':
        return React.createElement(FacebookWorkplace, {
          size: props.size,
          color: props.color
        });

      case 'instagram':
        return React.createElement(Instagram, {
          size: props.size,
          color: props.color
        });

      case 'whatsapp':
        return React.createElement(WhatsApp, {
          size: props.size,
          color: props.color
        });

      case 'telegram':
        return React.createElement(Telegram, {
          size: props.size,
          color: props.color
        });

      case 'flag':
        return React.createElement(Flag, {
          size: props.size,
          color: props.color
        });

      case 'star-half-full':
        return React.createElement(StarHalfFull, {
          size: props.size,
          color: props.color
        });

      case 'plus':
        return React.createElement(Plus, {
          size: props.size,
          color: props.color
        });

      case 'account-plus':
        return React.createElement(AccountPlus, {
          size: props.size,
          color: props.color
        });

      case 'share':
        return React.createElement(Share, {
          size: props.size,
          color: props.color
        });

      case 'phone':
        return React.createElement(Phone, {
          size: props.size,
          color: props.color
        });

      case 'bell':
        return React.createElement(Bell, {
          size: props.size,
          color: props.color
        });

      case 'wrench':
        return React.createElement(Wrench, {
          size: props.size,
          color: props.color
        });

      case 'minus':
        return React.createElement(Minus, {
          size: props.size,
          color: props.color
        });

      case 'check-circle':
        return React.createElement(CheckCircle, {
          size: props.size,
          color: props.color
        });

      default:
        return '';
    }
  };

  var iconClass = function iconClass() {
    if (props.className) return "rui-icon ".concat(props.className);else return 'rui-icon';
  };

  return React.createElement("i", {
    title: props.title,
    style: {
      width: props.size ? props.size : 24,
      height: props.size ? props.size : 24
    },
    className: iconClass(),
    onMouseOver: function onMouseOver(e) {
      return props.onMouseOver ? props.onMouseOver(e) : {};
    },
    onMouseLeave: function onMouseLeave(e) {
      return props.onMouseOver ? props.onMouseLeave(e) : {};
    },
    onClick: function onClick(e) {
      return props.onClick ? props.onClick(e) : {};
    }
  }, React.createElement(Icon, null));
};

export default Icon;