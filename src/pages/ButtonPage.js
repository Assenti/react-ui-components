import React from 'react';
import { Button } from '../components/Button';

export const ButtonPage = () => {
    return (
        <div className="page">
            <Button
                color="primary"
                name="Button"
                onAction={() => {}}/> 
        </div>
    )
}
